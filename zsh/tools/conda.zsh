# ~/.dotfiles/zsh/tools/conda.zsh
# Miniconda initialization.
#
# IMPORTANT: On every miniconda upgrade:
#   1. Comment out the source line below.
#   2. Upgrade the package as usual.
#   3. Uncomment the line after upgrading.

# [ -f /opt/miniconda3/etc/profile.d/conda.sh ] && source /opt/miniconda3/etc/profile.d/conda.sh
